# file-preview-kit 项目进度文档

> 配套文档：`项目方案.md`（需求规格 v2.1）、`packages/core/TDD-REPORT.md`（验证报告）。
> 本文件记录「现状对账 → 缺口分析 → 下一步建议 → 路线」，作为后续迭代的单一进度源。
> 最近更新：2026-08-26（Milestone A/B + D1 + C1/C2 + D2 + D3 收口；**C3 分包收口，方案后置清单清空**）· 基于 TDD-REPORT §八/§九/§十/§十一/§十三/§十四/§十五/§十六 的独立复验结论。

---

## 一、当前状态（一句话）

**核心引擎与全部点名格式（image / text / json / markdown / csv / xml / Office docx·xlsx·pptx / PDF / zip+炸弹防御 / **媒体元数据 mediainfo** / **邮件 eml**；font/3d/msg 按方案声明 binary 降级边界）已实现、真实跑通、且与方案各节承诺一致；P0+P1 全绿，方案格式章节 §5.1–§5.7 **全部闭合**。Milestone A/B/D1/C1/C2/D2/D3 均已于 2026-08-25 交付并复验；**C3 分包已于 2026-08-26 收口**——pdf/office/archive 拆为 `@file-preview/plugin-pdf|office|archive` 独立按需包（§9 多包结构落地），方案全部待办清空。**

复验证据（2026-08-26 C3 收口重跑）：`typecheck×4 包=0` / `BUILD=0`（零警告）/ `vitest 125/125=0` / `SMOKE=0` / `verify:browser 探针=0` / `DIST 生产路径探针=0` / `CJS require OK 且 pdfPlugin 零泄漏` / `SIZE=0`。主入口 index.cjs gz **115.1K→96.5K（-16%）**。详见 `TDD-REPORT.md` §十六。

---

## 二、文档要求 vs 现状 对账

| 方案节 | 要求 | 现状 | 判定 |
|---|---|---|---|
| §1–§3 核心 | 流式 IFile / EnvAdapter / 插件路由 / Worker 统一派发 | 全部落地，源码核对通过 | ✅ |
| §4 探测 | 魔数+扩展名+PK头+EOCD 细分 docx/xlsx/pptx | 落地（用自研 MAGIC 表替代 `file-type`，效果等价） | ✅ |
| §5.1 图片 | dataURL / 尺寸 / SVG `<img>` | 落地 | ✅ |
| §5.2 PDF | 浏览器 canvas / Node 文本提取 | 落地（Node legacy 文本 + 真机 canvas 实证） | ✅ |
| §5.3 Office | docx→mammoth / xlsx→exceljs / **pptx→fflate+XML** | 全部 ✅（pptx 于 2026-08-25 收口：fflate 解压 slide XML + `<a:t>` 抽取 → html，含解压预算防炸弹；见 `office.ts` / `tests/office.test.ts`） | ✅ |
| §5.4 压缩包 | zip→tree + 四阈值炸弹防御 | 落地（fflate filter 恒 false 永不解压） | ✅ |
| §5.5 文本族 | text/md/csv/json + xml | 全部 ✅（xml 于 2026-08-25 收口：fast-xml-parser → kind:json；DOCTYPE 剥离 + XMLValidator 严格校验，XXE/实体炸弹免疫；见 `plugins/xml.ts` / `tests/xml.test.ts`） | ✅ |
| §5.6 媒体 | mediainfo.js WASM 元数据 | 全部 ✅（media 于 2026-08-25 收口：mediainfo.js@0.3.1 动态 import external + analyzeData 流式区间读取 → kind:'media'；浏览器 createObjectURL 播放 src / Node 仅 metadata；轨道判定以 Audio·Video 为准防误判；见 `plugins/media.ts` / `tests/media.test.ts`） | ✅ |
| §5.7 邮件/字体/3D | eml 结构化解析；font/3d/msg 声明边界 | 邮件 ✅（eml 于 2026-08-25 收口：emailjs-mime-parser → kind:'html' 头表+正文+附件清单；HTML-only 转义源码安全边界；见 `plugins/email.ts` / `tests/email.test.ts`）；font/3d/msg 按方案走 binary 降级并在 README「格式支持边界」声明 | ✅ |
| §6 路由/缓存 | 归一 number 路由 + LRU 缓存 | 落地（含 `maxBytes` 护栏、combineSignal 超时） | ✅ |
| §7 渲染 | sanitize 唯一清理点 / iframe 隔离 / media 分支 | 落地（浏览器+Node 双渲染器，media 已补） | ✅ |
| §9 打包 | pnpm 多包 + changesets + tsup ESM/CJS | **npm workspaces 四包**（core + plugin-pdf/office/archive）各自出 ESM/CJS/DTS；**changesets ✅（2026-08-25：config+initial changeset+`version` 实证 0.1.0→0.2.0+CHANGELOG）**；**多 plugin-* 分包 ✅（2026-08-26 C3 收口：三重插件独立按需包，源码级 exports 免构建排序，browser/worker 零构建默认集保持完整；CI `--workspaces` 自动覆盖）** | ✅ |
| §12 测试 | sanitize 回归 / 炸弹 fixture / golden-file / 体积预算 / changesets | 全部 ✅（**体积预算 CI 于 2026-08-25 收口**：`scripts/check-size.mjs` 五入口 gzip 逐限 + chunks 聚合 + dist 总量，正/负双向验证；`.github/workflows/ci.yml` 串联 typecheck→vitest→build→smoke→探针→size 卡点） | ✅ |
| §14 零构建 | `@file-preview/browser` ESM + Web Component | 落地（`browser.js` + `customElements.define`） | ✅ |
| §16 生产就绪 | 错误码 / 超时 / **CSP 文档** / 结构化日志 | 全部 ✅（CSP 指南见 `docs/csp-guide.md`） | ✅ |
| 工程缺陷（复验新发现） | — | **已修复（2026-08-25，TDD-REPORT §九.2）**：browser 仅出 ESM、`index.cjs` require 崩溃修复、构建零 `import.meta` 警告 + 回归护栏 | ✅ |

**达标率（可落地承诺）**：核心调度 100% / 安全 100% / 分发 100% / 格式覆盖 100%（方案点名格式 §5.1–§5.7 全闭合） / 工程化 100%（changesets + CI 全链路卡点 + C3 多包分包收口）。生产可用视角 P0+P1 全绿且四项生产就绪要求齐备，完成度 **100%「生产可发布」级别**——方案可落地承诺全部交付。

---

## 三、下一步建议（发布主线 Release Track）

方案全部里程碑已交付（§二 对账全 ✅，TDD-REPORT §十六 复验收口）。当前唯一阻塞链路是**版本治理基础设施**——仓库尚未纳入 git，导致三件已建成的设施空转（changesets 无 git 实测报错、CI 流水线无法触发、发布无从谈起）。推荐按序推进：

1. **git 仓库初始化（P0 · 阻塞项）**
   - `.gitignore` 就绪 → `git init` + 初始提交
   - 立即解锁：changesets 消费、CI 七步流水线、版本历史与回滚
2. **版本消费与发布干跑（P1）**
   - `npm run version`：消费 3 条待处理 changeset（c3-plugin-split / email / media）→ 四包 semver bump + CHANGELOG 自动生成
   - 各包 `npm pack --dry-run` 核对发布内容：插件包顶层 `files: []` 依赖 `publishConfig.files:["dist"]` 覆盖 + exports 由 src 切 dist，需实证一次再发布
   - 具备 npm 凭据与远端仓库后 `npm run release`（CI 中由 changesets/action 代办）
3. **可选增强（P3 · 均非方案欠账）**
   - TDD-REPORT §16.5 已知边界加固：text 嗅探 strict 化（fatal decode 或控制字节比例阈值）、importmap 用法写入 `docs/csp-guide.md`
   - `@file-preview/browser` 独立 CDN 包抽取（README 标注规划中，当前由 core 的 `/browser` 零构建入口承担）

---

## 四、建议的下一次迭代范围（验收清单）

**Milestone A — 生产就绪收口（✅ 2026-08-25 已完成，证据见 TDD-REPORT §九）**
- [x] 新增 CSP 部署文档（§16.3），含最小 CSP 片段 + 自托管 wasm/worker 说明 → `docs/csp-guide.md` + README「Hosting / CSP」
- [x] 修复/规避 tsup CJS `import.meta` 警告，确保 CJS 消费者可解析 worker URL（并补一条冒烟断言）→ browser 仅出 ESM；`index.cjs` require 崩溃一并修复；`tests/build-clean.test.ts` 四断言护栏
- [x] 新增 golden-file 回归测试套件（fixtures + 快照）→ `tests/golden/`（9 格式 10 用例，fixtures 确定性生成并提交）
- [x] 复验：`npm test`（91/91）/ `npm run smoke` / dist 探针全绿，TDD-REPORT 同步更新（§九）

**Milestone B — Office 收口（✅ 2026-08-25 已完成，证据见 TDD-REPORT §十）**
- [x] `office.ts` 增加 pptx 分支（fflate 解压 + slide XML 抽取 → html）→ `previewPptx`：仅解压 `ppt/slides/slideN.xml`、按中央目录声明值做 64MB 解压预算；`<a:p>/<a:t>` 分段抽取 + 实体还原后转义
- [x] `officePlugin().test()` 对 `zipHint==='pptx'` 返回优先级；`.pptx` 不再降级 binary → test() 三 zipHint + 六扩展名均返 90；候选链（docx/xlsx/pptx 互退）兼容改名场景
- [x] vitest fixture（真实最小 pptx）+ 错误码/降级回归 → `tests/office.test.ts` 8 用例（内联 zipSync 确定性 fixture）：正常 html / 损坏 ERR_PARSE / 管线级 binary 降级契约 / 路由互斥 / 无扩展名兜底

**Milestone C — 工程化（方案 §9/§12 收尾，P2）**
- [x] changesets 版本治理 + CHANGELOG → **C1 ✅ 2026-08-25**：`.changeset/{config.json,README.md}` + 根脚本 `changeset/version/release` + initial changeset 实证（0.1.0→0.2.0，CHANGELOG 自动生成）；contractVersion 联动规则文档化于 `.changeset/README.md`
- [x] CI 体积预算卡点（每插件产物上限）→ **C2 ✅ 2026-08-25**：`packages/core/scripts/check-size.mjs`（五入口 gzip 逐限 + hash 命名动态 chunk 聚合 170K + dist 总量 1.8M raw 护栏；正/负双向实证 exit 0/1）+ `.github/workflows/ci.yml`（push/PR 触发：npm ci → typecheck → vitest → build → smoke → dist 探针 → size 卡点）
- [x] （C3）按 §9 将 pdf/office/archive 拆为独立 `plugin-*` 包 → **C3 ✅ 2026-08-26**：`@file-preview/plugin-pdf`（pdfjs-dist）/ `plugin-office`（mammoth·exceljs）/ `plugin-archive`（fflate）三独立包；core 默认集收窄为轻量七插件、重依赖随插件迁出（index.cjs gz -16%）；browser/worker 零构建入口源码级内联保持默认集完整；受影响测试改显式组合；check-size 新基线 + 八门全绿。证据见 TDD-REPORT §十六

---

## 五、结论

代码**已经达到文档要求的主体效果**（核心调度、安全、分发、主流格式全覆盖且真跑通）。**Milestone A、B、D1、C1、C2、D2、D3 均已于 2026-08-25 交付复验，C3 分包已于 2026-08-26 收口**——方案 §12 测试与发布五项要求全数落地、格式章节（§5.1–§5.7）全部闭合、§9 多包结构落地。**方案全部待办清空，无可选项剩余。**
