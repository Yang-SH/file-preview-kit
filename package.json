{
  "name": "file-preview-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Isomorphic file-preview component library (monorepo root)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "build:browser-package": "node scripts/build-offline-browser-package.mjs",
    "test:e2e": "npm run build --workspaces --if-present && vitest run packages/core/tests/e2e && echo \"[e2e] Node 接缝完成；浏览器接缝请伺服 packages/core 后打开 /tests/e2e/browser-e2e.html（先运行：node packages/core/tests/e2e/gen-browser-harness.mjs）\"",
    "smoke": "node --experimental-strip-types packages/core/examples/node-ssr/smoke.ts",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "@types/node": "^26.2.0",
    "@types/papaparse": "^5.5.2",
    "vitest": "^4.1.11"
  },
  "license": "MIT",
  "dependencies": {
    "fflate": "^0.8.3",
    "markdown-it": "^15.0.0",
    "papaparse": "^5.6.0"
  }
}
