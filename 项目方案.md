下面给出一套 **纯前端 JS/WASM 的常见文件预览组件** 的可行方案（v2 优化版）。核心思路不变：

> **同构核心 + 环境适配层 + 插件式解析器 + 统一结果模型 + 双渲染器**
> 浏览器端渲染为 DOM，Node 端渲染为 HTML 字符串直出浏览器，所有解析均不依赖服务端和原生二进制。

相对 v1 的关键修正（已落在本方案各章节）：
- `IFile` 改为**流式/按需读取**，消解"整文件进内存"与"大文件流式"的自相矛盾；
- 类型探测补充 `file-type` v17 **已移除 zip/Office 探测**的兜底；
- 安全从"口头要求"改为**渲染层强制 sanitize** 的纵深防御，并明确 `html` 与 `iframe` 两种路径的边界；
- 替换 `xlsx`(SheetJS) 的许可证地雷，改用 `exceljs`(MIT)；
- 修正 `EnvAdapter.runWorker` 的错误签名、插件 `test()` 的隐式类型转换、`renderToHtml` 漏掉 `media` 分支等接口级缺陷；
- 新增 **progress/AbortSignal 契约、缓存层、测试策略、开源发布/版本治理**。

> v2.1 核验修正（按"可实现 / 不复杂 / 生产可用"三轴复审）：Worker 改为**核心统一派发**模型；`maxBytes` 在 router 强制短路（防大 Office 爆内存）；SVG `<img>` 模式告警修正（既挡脚本也挡外链，无需剥离）；Node sanitize 改 `sanitize-html` 避开 jsdom；新增**第十六节"生产就绪"**（错误码 / 超时 / CSP / 日志）；mediainfo.js 经核实为 **BSD-2-Clause**（干净）但标注停更风险。

---

## 一、总体架构

```
输入文件
  │
  ├─ 浏览器：File / Blob / Response
  └─ Node：文件路径 / Buffer / Stream
        │
        ▼
[1] 环境适配层 EnvAdapter          ← 抽离 DOM/window/fs/WASM/Worker/ObjectURL/sanitize
        │
        ▼
[2] 文件类型探测 Detect            ← 魔数 + 扩展名 + MIME + zip 内部探测兜底
        │
        ▼
[3] 插件路由 Plugin Router         ← 按归一化优先级匹配，支持按 AbortSignal 中断
        │
        ▼
[4] 具体解析插件 Parser（纯 JS / WASM）← 仅依赖 IFile + EnvAdapter + PreviewOptions
        │
        ▼
[5] 统一 PreviewResult
        │
        ├─ 浏览器：render(result, container) → 内部先 sanitize 再 DOM（或 iframe 隔离）
        └─ Node：renderToHtml(result) → 服务端 sanitize 后产出完整 HTML 直出
```

关键点：

- **核心层不依赖 DOM、window、document**；所有环境差异经 `EnvAdapter` 抽离（含 sanitize 实现）。
- 每种文件类型是一个插件，可动态加载、按需引入，避免主包过大。
- 解析结果统一为 `PreviewResult`，解析层**不碰 DOM、不拼最终 HTML**；渲染层是唯一允许 sanitize/插 DOM 的地方。

---

## 二、核心抽象

### 1. 统一文件模型（流式优先）

v1 的 `data: Uint8Array | ArrayBuffer` 强制整文件进内存，与"大文件只读头部"冲突。改为**按需区间读取**，`arrayBuffer()` 仅作为小文件快捷路径。

```ts
interface IFile {
  readonly name: string;
  readonly size: number;
  mimeType?: string;
  extension?: string;

  /** 探测用：读文件头部（默认前 16KB），不加载全量 */
  header(maxBytes?: number): Promise<Uint8Array>;
  /** 按需区间读取，支持流式/大文件；插件不应假设整文件已在内存 */
  readRange(start: number, end: number): Promise<Uint8Array>;
  /** 懒加载整文件（小文件快捷路径）；大文件插件慎用 */
  arrayBuffer(): Promise<ArrayBuffer>;
}
```

浏览器端（基于 `Blob.slice`，天然零拷贝区间读取）：

```ts
async function fileFromBrowser(file: File | Blob): Promise<IFile> {
  return {
    name: (file as File).name ?? 'blob',
    size: file.size,
    mimeType: (file as File).type || undefined,
    async header(maxBytes = 16 * 1024) {
      return new Uint8Array(await file.slice(0, maxBytes).arrayBuffer());
    },
    async readRange(start, end) {
      return new Uint8Array(await file.slice(start, end).arrayBuffer());
    },
    async arrayBuffer() {
      return file.arrayBuffer();
    }
  };
}
```

Node 端（基于 `fs.open` + `fd.read`，避免一次性 `readFile`）：

```ts
import fs from 'node:fs/promises';
import path from 'node:path';

async function fileFromNode(filePath: string): Promise<IFile> {
  const stat = await fs.stat(filePath);
  const fd = await fs.open(filePath, 'r');
  return {
    name: path.basename(filePath),
    size: stat.size,
    async header(maxBytes = 16 * 1024) {
      const buf = Buffer.alloc(Math.min(maxBytes, stat.size));
      await fd.read(buf, 0, buf.length, 0);
      return new Uint8Array(buf);
    },
    async readRange(start, end) {
      const len = end - start;
      const buf = Buffer.alloc(len);
      await fd.read(buf, 0, len, start);
      return new Uint8Array(buf);
    },
    async arrayBuffer() {
      const buf = await fs.readFile(filePath);
      return buf.buffer.slice(buf.byteOffset, buf.byteOffset + buf.byteLength) as ArrayBuffer;
    }
  };
}
```

### 2. 统一预览结果

```ts
type PreviewResult =
  | { kind: 'html';   html: string; title?: string }                 // 片段/已清理 HTML，渲染层再 sanitize
  | { kind: 'text';   text: string; language?: string }              // 纯文本 + 语言（高亮用）
  | { kind: 'image';  dataUrl: string; width?: number; height?: number; mimeType?: string }
  | { kind: 'media';  mediaType: 'video' | 'audio'; src?: string; mimeType?: string; metadata?: Record<string, unknown> }
  | { kind: 'table';  columns: string[]; rows: unknown[][]; sheetName?: string }
  | { kind: 'tree';   nodes: FileTreeNode[] }
  | { kind: 'json';   data: unknown }
  | { kind: 'binary'; hexDump?: string; info?: Record<string, unknown> }
  | { kind: 'iframe'; srcdoc?: string; sandbox?: string[] }          // 完整文档级 HTML，iframe 隔离
  | { kind: 'error';  message: string; code?: string };
```

`html` 与 `iframe` 的边界（v1 缺失，补齐）：
- `kind:'html'`：可信片段或已清理内容，渲染层会**再 sanitize 一次**（纵深防御）。
- `kind:'iframe'`：Office/markdown 转出的**整页文档级 HTML**，必须用 `<iframe sandbox>`/`srcdoc` 隔离，不进入主 DOM。

### 3. 插件接口（含 progress / AbortSignal）

v1 的 `test()` 返回 `boolean | number` 靠 `true > 0` 隐式成立，易误用；且插件无法上报进度、无法取消。修正如下：

```ts
interface PreviewOptions {
  signal?: AbortSignal;                       // 取消解析
  timeout?: number;                           // 默认 30000ms，超时返回 ERR_TIMEOUT
  onProgress?: (p: { phase: string; loaded: number; total?: number }) => void;
  maxBytes?: number;                          // 超过此值降级为元数据/十六进制（默认 100MB）
}

interface PreviewPlugin {
  id: string;
  contractVersion: 1;                         // 插件契约版本，破坏性变更时递增
  /** 返回 0=不匹配；>0 优先级，越大越优先（归一为 number，杜绝布尔隐式转换） */
  test(ctx: DetectResult): number;
  preview(file: IFile, env: EnvAdapter, opts: PreviewOptions): Promise<PreviewResult>;
}
```

---

## 三、环境适配层

同构关键。除 v1 的 `loadWasm`/`createObjectURL`/`revokeObjectURL` 外，补两处：**Worker 跑解析**和**sanitize 实现**都经它抽离（否则 Node 端无法复用同一套渲染安全逻辑）。

> ⚠️ v1 的 `runWorker(fn: () => void)` 是错的——Web Worker 不能接收函数。改为基于消息的 worker 句柄。

```ts
interface WorkerHandle {
  post<T>(payload: unknown, opts?: { signal?: AbortSignal }): Promise<T>;
  terminate(): void;
}

interface EnvAdapter {
  isBrowser: boolean;
  loadWasm(url: string): Promise<ArrayBuffer | WebAssembly.Module>;
  createObjectURL(data: Blob | Uint8Array, mimeType: string): string | null;
  revokeObjectURL(url: string): void;
  /** 在 Worker 中执行重解析任务，避免阻塞 UI；Node 端可返回 null（主线程异步即可） */
  spawnWorker?(workerUrl: string): WorkerHandle | null;
  /** 统一 sanitize 入口：浏览器走 DOMPurify，Node 走 sanitize-html */
  sanitize(html: string, opts?: { iframe?: boolean }): string;
  /** 可选结构化日志钩子：生产环境注入上报（见第十六节） */
  log?(level: 'info' | 'warn' | 'error', msg: string, err?: unknown): void;
}
```

Node 实现要点：

```ts
import sanitizeHtml from 'sanitize-html'; // Node 端纯字符串清洗，无 jsdom 依赖；浏览器端仍用 DOMPurify

const nodeAdapter: EnvAdapter = {
  isBrowser: false,
  async loadWasm(url) { /* fs.readFile → ArrayBuffer */ },
  createObjectURL() { return null; },            // Node 无 Blob URL，用 data URL
  spawnWorker() { return null; },                // Node 主线程异步即可
  sanitize: (html, opts) => sanitizeHtml(html, opts?.iframe ? IFRAME_CONFIG : HTML_CONFIG)
};
```

这样**所有解析插件只依赖 `EnvAdapter`**，渲染层经 `env.sanitize` 做唯一清理点，杜绝 v1 "代码没调 sanitize" 的漏洞。

### Worker 派发模型（核心统一派发）
`IFile`（含闭包方法）与 `EnvAdapter`（含函数）均**不可结构化克隆**，无法直接 `postMessage` 给 Worker。因此采用统一派发：主线程只把**原始 `File`/`Blob` + 探测结果 `DetectResult` + `PreviewOptions`** 发给 Worker；Worker 内用 `fileFromBrowser(blob)` 重建 IFile、用 worker 版 `EnvAdapter`（无 sanitize）跑匹配的插件，返回**纯数据** `PreviewResult`；主线程拿到后再经 `render()`/`renderToHtml()` 的 `env.sanitize` 统一清理并渲染（sanitize 不进 Worker，避开 Worker 无 DOM 的问题）。kit 随包提供 `preview.worker`（导入默认插件集），消费者无需自建。

---

## 四、文件类型探测

先探测类型，再选插件。v1 依赖 `file-type` 但**未说明其 v17+ 已移除 zip/Office 探测**——docx/xlsx/pptx 全是 zip，会直接漏检到扩展名兜底；重命名文件（无扩展名）则彻底漏检。

```ts
import { fileTypeFromBuffer } from 'file-type'; // 仅覆盖图片/音视频/部分二进制
import mime from 'mime-types';

async function detectFile(file: IFile): Promise<DetectResult> {
  const header = await file.header();
  const detected = await fileTypeFromBuffer(header);

  // 兜底：file-type 对 zip/Office 返回 null，用 PK 头 + 扩展名补
  const isZip = header[0] === 0x50 && header[1] === 0x4b; // "PK"
  let zipHint: DetectResult['zipHint'] = null;
  if (isZip) {
    zipHint = classifyZip(file); // 见下：按需读 [Content_Types].xml 区分 docx/xlsx/pptx
  }

  return {
    mimeType: detected?.mime ?? file.mimeType ?? mime.lookup(file.name) ?? 'application/octet-stream',
    extension: detected?.ext ?? file.extension ?? path.extname(file.name).slice(1),
    fileName: file.name,
    header,
    zipHint
  };
}
```

zip 家族细分（按扩展名即可；无扩展名时需解析中央目录）：

```
PK\x03\x04 命中 → 是 zip 家族
  ├─ 扩展名 .docx/.docm            → docx 插件
  ├─ 扩展名 .xlsx/.xlsm            → xlsx 插件
  ├─ 扩展名 .pptx/.pptm            → pptx 插件
  └─ 无扩展名时：读文件尾部 64KB 定位 EOCD，解析中央目录条目名
        ppt/  → pptx；xl/ → xlsx；word/ → docx；否则普通 zip
```
> 注：无扩展名细分需解析 zip 中央目录（非"读少量字节"），由 `classifyZip` 用 `readRange` 读尾部实现；若只需粗分，可仅判 PK 头即归为通用 zip（走 tree 插件）。

降级策略：无法识别 → 尝试 UTF-8 解码（可读率高则文本预览），否则十六进制转储前 64KB。

---

## 五、常见文件类型实现路线

### 1. 图片：png / jpg / gif / webp / bmp / svg

- 生成 Data URL：`data:${mime};base64,...`
- 浏览器端缩略图：`createImageBitmap` / `Image` + Canvas
- Node 端尺寸：`image-size`（纯 JS）
- SVG：用 `<img src="data:image/svg+xml;base64,...">`。**浏览器对 `<img>` 模式的 SVG 强制阻断脚本执行与外链资源加载**（MDN/规范确认），因此 `<img>` 天然安全，无需剥离 `foreignObject`/外链。⚠️ 仅当必须把 SVG **内联进 DOM**（`innerHTML`）时才需经 `env.sanitize` 清洗。

### 2. PDF

`pdfjs-dist` 同构，但**浏览器必须配置 worker**，v1 完全漏写：

```ts
import * as pdfjsLib from 'pdfjs-dist';
// 浏览器：指定 worker（Vite 下用 new URL 解析）
pdfjsLib.GlobalWorkerOptions.workerSrc = new URL(
  'pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url
).href;
const doc = await pdfjsLib.getDocument({ data: await file.arrayBuffer(), isEvalSupported: false }).promise;
```

- Node 端默认只出文本 + 页数 + 元数据；文本提取支持 `opts.signal` 中断、`onProgress` 上报页数进度。
- Node 端若要页面缩略图，接 `canvaskit-wasm`（纯 WASM）而非 `@napi-rs/canvas`（原生模块，违背"无原生依赖"）。

### 3. Office 文档

#### docx
`mammoth.convertToHtml` → `kind:'html'`。
⚠️ **版式还原有限**（v1 只给 pptx 标了 caveat，docx 同样要标）：丢页眉页脚、文本框、批注、多数样式。**适合"内容预览"，不适合"版式还原"**。

#### xlsx / xls
⚠️ **许可证地雷**：v1 推荐的 `xlsx`(SheetJS) npm 社区版停在 2022 v0.18.5（事实弃用），新版走付费 CDN、许可证收紧。
✅ 改用 `exceljs`（MIT，读 fidelity 更好）：

```ts
import ExcelJS from 'exceljs';
const wb = new ExcelJS.Workbook();
await wb.xlsx.load(await file.arrayBuffer());
const ws = wb.getWorksheet(1);
const rows = ws.getRows(1, Math.min(ws.rowCount, 1000)) ?? [];
// 组装 { kind: 'table', columns, rows } 或自拼最小 HTML
```

如需写入/双向，则 `exceljs` 更合适；纯读场景 `exceljs` 完全够用且许可证干净。

#### pptx
zip + XML。用 `fflate` 解压 → 解析 `ppt/slides/slideN.xml`。
⚠️ 版式还原有限，适合"内容预览"。

### 4. 压缩包：zip / tar / gz / 7z

- `fflate`：zip/gz/zlib，同构极快
- `libarchive.js`：WASM，支持更多格式（7z/rar）
- 输出 `kind:'tree'`

⚠️ **zip 炸弹防御（给具体阈值，v1 只有口号）**：
- 解压后总大小 ≤ 100MB
- 条目数 ≤ 1000
- 嵌套层数 ≤ 10
- 单条目解压上限，超限立即中止并降级

### 5. 文本 / 代码 / Markdown / CSV / JSON / XML

- 纯文本：`TextDecoder` 解码；编码检测 `jschardet` **只对样本（前 64KB）跑**，避免全文件扫描卡顿。
- 语法高亮：`highlight.js`；⚠️ `highlightAuto` 对超大文件有性能/ReDoS 风险 → 限长（如 512KB）或指定 language。
- Markdown：`markdown-it`/`marked` → `kind:'html'`，渲染层 sanitize。
- CSV：`papaparse` → `kind:'table'`。
- JSON/XML：`fast-xml-parser`（默认禁实体扩展，天然防 XXE）。

### 6. 音频 / 视频

`mediainfo.js`（WASM）提取元数据：
```ts
const mediaInfo = await MediaInfoFactory.createMediaInfo({ locateFile: (p) => env.loadWasm(p) });
const info = await mediaInfo.analyzeData(
  () => file.size,
  (chunkSize, offset) => file.readRange(offset, offset + chunkSize)
);
```
浏览器端：`env.createObjectURL` 生成播放 src → `kind:'media'`。Node 端：仅 `metadata`，不播放。

> mediainfo.js 许可证为 **BSD-2-Clause**（干净，无 GPL 污染），但上游基本停更（OpenSSF 评分 3.7/10）。生产使用：**锁定版本**（如 `mediainfo.js@0.3.1`）并把 `MediaInfoModule.wasm` 随包固定托管；对可用性敏感的场景可评估 `music-metadata`（仅音频，MIT）作为补充。

### 7. 字体 / 3D / 邮件 / 未知文件
同 v1，补充：eml 用 `emailjs-mime-parser`；msg 暂不覆盖（纯 JS 解析成本高）；未知文件降级同第四节。

---

## 六、插件与路由示例

```ts
class Previewer {
  private plugins: PreviewPlugin[] = [];
  register(plugin: PreviewPlugin) { this.plugins.push(plugin); }

  async preview(file: IFile, env: EnvAdapter, opts: PreviewOptions = {}): Promise<PreviewResult> {
    const detected = await detectFile(file);

    // 0) 大小护栏：先于任何插件，防 office 插件整文件 arrayBuffer() 爆内存
    const maxBytes = opts.maxBytes ?? 100 * 1024 * 1024;
    if (file.size > maxBytes) return this.fallback(file, detected, 'ERR_TOO_LARGE');

    // 1) 默认超时合并到 signal（防坏/恶意文件挂起主线程或 Worker）
    const signal = combineSignal(opts.signal, opts.timeout ?? 30000);

    const matches = this.plugins
      .map(p => ({ p, priority: p.test(detected) }))
      .filter(x => x.priority > 0)                     // 归一 number，无布尔隐式
      .sort((a, b) => b.priority - a.priority);

    for (const { p } of matches) {
      try {
        const r = await p.preview(file, env, { ...opts, signal });
        if (r.kind !== 'error') return r;
      } catch (e) {
        if (signal.aborted) throw new PreviewAbortError();   // 含超时取消
        env.log?.('error', `[preview] plugin ${p.id} failed`, e); // 结构化日志（见十六节）
      }
    }
    return this.fallback(file, detected, 'ERR_UNSUPPORTED');
  }

  private fallback(file: IFile, detected: DetectResult, code: string): PreviewResult {
    return { kind: 'binary', hexDump: generateHexDump(await file.readRange(0, 64 * 1024)),
             info: { size: file.size, mimeType: detected.mimeType, code } };
  }
}
// combineSignal：把 opts.signal 与 AbortSignal.timeout(opts.timeout) 合并（实现略）
```

可选**缓存层**（v1 缺失）：按 `name+size+sha1(header)` 做 LRU，同文件二次预览直接命中 `PreviewResult`。⚠️ 仅缓存 `text`/`table`/`tree`/`json`/`media(metadata)` 等轻量结果，**跳过 `image`/`media(带 dataUrl)`**（base64 体积大，缓存反而吃内存）。

---

## 七、渲染层

### 浏览器（vanilla DOM，框架无关）

```ts
function render(result: PreviewResult, container: HTMLElement, env: EnvAdapter) {
  switch (result.kind) {
    case 'html':
      container.innerHTML = env.sanitize(result.html);          // 唯一清理点
      break;
    case 'iframe': {
      const f = document.createElement('iframe');
      f.sandbox.value = (result.sandbox ?? ['allow-same-origin']).join(' ');
      f.srcdoc = env.sanitize(result.srcdoc ?? '');
      container.appendChild(f);
      break;
    }
    case 'image': { /* img.src = dataUrl */ break; }
    // text / table / tree / json / media / binary ...
  }
}
```

### Node（SSR 直出 HTML）

```ts
function renderToHtml(result: PreviewResult, env: EnvAdapter): string {
  switch (result.kind) {
    case 'html':   return `<div class="fp-html">${env.sanitize(result.html)}</div>`;
    case 'iframe': return `<iframe sandbox="allow-same-origin" srcdoc="${escapeAttr(env.sanitize(result.srcdoc ?? ''))}"></iframe>`;
    case 'text':   return `<pre class="fp-text">${escapeHtml(result.text)}</pre>`;
    case 'image':  return `<img src="${result.dataUrl}" alt="" />`;
    case 'table':  return tableToHtml(result);
    case 'media':  return mediaToHtml(result);                  // ← v1 漏掉，已补
    case 'tree':   return treeToHtml(result.nodes);
    case 'json':   return `<pre>${escapeHtml(JSON.stringify(result.data, null, 2))}</pre>`;
    default:       return `<pre>${escapeHtml(JSON.stringify(result, null, 2))}</pre>`;
  }
}
```

安全要点（已从"口号"落到代码）：
- `html`/`iframe` 一律经 `env.sanitize`（浏览器 DOMPurify / Node isomorphic-dompurify）。
- 不可信整页 HTML（Office/markdown 转出）走 `iframe + sandbox`，不进主 DOM。
- SVG 用 `<img>` 模式天然安全（浏览器阻断脚本与外链）；仅内联 SVG 才需 `env.sanitize`。
- 不执行 Office 宏、PDF JS；`fast-xml-parser` 默认禁实体扩展防 XXE。

---

## 八、WASM 资源加载

同 v1，补充：浏览器端 `pdfjs` 的 worker 必须随包发布（见第九节打包）。

```ts
async function loadWasmAsset(env: EnvAdapter, url: string) {
  return env.loadWasm(url); // 浏览器 fetch / Node fs.readFile，由 adapter 决定
}
```

Vite：
```ts
export default { assetsInclude: ['**/*.wasm'] };
```
Node：`fs.readFileSync(new URL('./file.wasm', import.meta.url))`。

---

## 九、项目结构与打包（开源组件库）

目标：框架无关、按需加载、严格版本治理。工具链：**pnpm workspace + changesets + tsup（ESM/CJS 双产物）**。

```
file-preview-kit/
├─ packages/
│  ├─ core/                 # 同构核心（types/detect/router/env/render）
│  ├─ plugin-pdf/           # pdfjs-dist（重，按需）
│  ├─ plugin-office/        # docx(xlsx via exceljs)/pptx
│  ├─ plugin-image/
│  ├─ plugin-media/         # mediainfo.js（WASM，按需）
│  ├─ plugin-archive/
│  ├─ plugin-text/
│  └─ plugin-fallback/
├─ fixtures/                # 测试用 golden 文件（见第十二节）
├─ examples/
│  ├─ browser/
│  └─ node-ssr/
└─ package.json             # workspace + changesets
```

发布约束：
- 每个包 `sideEffects: false`，便于 tree-shaking；重依赖（pdfjs、mediainfo）设为**可选/peer 或动态 import**，不进主包。
- `contractVersion` 写入插件元信息，破坏性变更递增并配合 semver major。
- `changesets` 管版本与 CHANGELOG；CI 自动发 npm。

---

## 十、完整使用示例

### 浏览器
```ts
import { createPreviewer, browserAdapter } from '@file-preview/core';
import { pdfPlugin } from '@file-preview/plugin-pdf';
import { officePlugin } from '@file-preview/plugin-office';

const previewer = createPreviewer({
  plugins: [pdfPlugin(), officePlugin(), imagePlugin(), textPlugin(), fallbackPlugin()]
});

input.addEventListener('change', async (e) => {
  const file = await fileFromBrowser(e.target.files[0]);
  const result = await previewer.preview(file, browserAdapter, { signal: controller.signal });
  render(result, document.getElementById('preview'), browserAdapter);
});
```

### Node（SSR 直出）
```ts
import { createPreviewer, nodeAdapter, renderToHtml } from '@file-preview/core';
import { pdfPlugin } from '@file-preview/plugin-pdf';

const previewer = createPreviewer({ plugins: [pdfPlugin(), officePlugin(), imagePlugin(), textPlugin()] });
const file = await fileFromNode('/path/to/document.pdf');
const result = await previewer.preview(file, nodeAdapter);
const html = `<!doctype html><html><body>${renderToHtml(result, nodeAdapter)}</body></html>`;
// 直出浏览器展示
```

---

## 十一、性能与安全注意事项（落地阈值）

### 性能
- 大文件走 `readRange` 区间读取，**不**一次性 `arrayBuffer()`（仅小文件快捷路径）。
- 浏览器重解析经 Worker（**核心统一派发模型**，见第三节）：主线程发 `File`/`Blob` → Worker 重建 IFile 解析 → 返回纯数据 → 主线程 sanitize+render；不阻塞 UI，支持 `opts.signal` 取消、`onProgress` 上报。
- 插件动态 import，减小首屏。
- 超过 `maxBytes`（默认 100MB）降级为元数据/十六进制。
- 可选 LRU 缓存按 `name+size+headerHash` 命中。

### 安全
- 所有 `html`/`iframe` 经 `env.sanitize`（DOMPurify / isomorphic-dompurify）。
- 不可信整页 HTML 走 `iframe sandbox`。
- SVG 用 `<img>` 模式天然安全（不执行脚本、不加载外链）；仅内联 SVG 需 sanitize。
- zip 炸弹：总大小 ≤100MB、条目 ≤1000、嵌套 ≤10。
- XXE：`fast-xml-parser` 默认禁实体扩展。
- 不执行宏/PDF JS；`highlightAuto` 限长防 ReDoS。
- 默认解析超时（见第十六节）+ 错误码体系；托管页需配置 CSP（见第十六节）。

---

## 十二、工程化：测试与发布（v1 完全缺失）

- **golden-file 测试**：每种格式放样例输入 + 期望 `PreviewResult` 快照（文本/表格/树），CI 比对。
- **sanitize 回归测试**：构造 XSS payload（`<script>`、`onerror=`、`<svg><foreignObject>`）断言被剥离。
- **zip 炸弹 fixture**：断言超限被降级、不 OOM。
- **体积预算**：每个插件产物（含 wasm）设 bundle size 上限，CI 卡点。
- **版本治理**：changesets + semver；`contractVersion` 与插件契约同步演进。
- **浏览器零构建包 `@file-preview/browser`**：见第十四节，ESM 模块 + Web Component，CDN 引入即用。

---

## 十三、落地优先级建议

1. **核心调度器 + 流式 IFile + EnvAdapter（含 sanitize/Worker）+ 探测（含 zip 兜底）**
2. **文本 / 图片 / JSON / Markdown**
3. **PDF（含 worker 配置）**
4. **Office：docx、xlsx（exceljs）**
5. **压缩包 zip（含炸弹防御）**
6. **媒体元数据**
7. **pptx / 字体 / 3D / 邮件**
8. **缓存（仅轻量结果）、Worker 化（核心统一派发）、SSR 直出、测试与发布流水线、生产就绪（错误码/超时/CSP/日志）**

---

## 十四、浏览器零构建引入（CDN Drop-in / Web Component）

目标：在**裸 HTML 文件**里，仅通过一行 `<script type="module">` 引入 CDN 构建，即可获得 `<file-preview>` 预览元素，**无需 npm、无需打包器、无需 Vite**（对应前面拍板的"代码可直接运行在浏览器中" + "捆绑核心默认集" + "列为 P0 分发模式"）。

> 机制选择：**ESM 模块 + Web Component**（框架无关、Shadow DOM 隔离，契合第二章"vanilla DOM"决策）。UMD/IIFE 全局脚本作为可选后续，非 P0。

### 1. 产物形态
- 独立包 `@file-preview/browser`（CDN 托管）：core + **默认插件集**（图片/文本/PDF/Office，重库按需懒加载） + `browserAdapter` 打成一个 ESM 模块。
- 模块副作用：导入即 `customElements.define('file-preview', ...)`，全局注册元素。
- 重依赖资源（pdfjs worker、mediainfo wasm）作为独立文件随包发布在 CDN 同目录 `assets/`。

### 2. 裸 HTML 用法
```html
<script type="module" src="https://cdn.example.com/file-preview/browser.js"></script>

<!-- 声明式 -->
<file-preview>
  <input type="file" slot="input" />
</file-preview>

<!-- 编程式 -->
<input type="file" id="f" />
<file-preview id="pv"></file-preview>
<script type="module">
  const pv = document.getElementById('pv');
  document.getElementById('f').addEventListener('change', async (e) => {
    await pv.preview(e.target.files[0]); // 内部 fileFromBrowser → previewer.preview → render
  });
</script>
```

### 3. Web Component 契约
```ts
class FilePreviewElement extends HTMLElement {
  preview(file: File | Blob): Promise<void>;  // 内部 fileFromBrowser → previewer.preview → render(this, browserAdapter)
  plugins?: PreviewPlugin[];                  // 可选覆盖默认集
}
customElements.define('file-preview', FilePreviewElement);
```
Shadow DOM 提供样式/脚本隔离；渲染仍走第七章 `render()`（含 `env.sanitize` 强制清理）。

### 4. WASM / Worker 在零构建下如何解析（关键修正）
v1 用 `new URL('pdfjs-dist/...', import.meta.url)` 在裸脚本会失效；但在 **ESM 模块**里 `import.meta.url` 就是模块自身的 CDN URL，因此**能正确解析**：
```ts
const CDN_BASE = new URL('./assets/', import.meta.url).href; // → https://cdn.example.com/file-preview/assets/
pdfjsLib.GlobalWorkerOptions.workerSrc = `${CDN_BASE}pdf.worker.min.mjs`;
MediaInfoFactory.createMediaInfo({ locateFile: (p) => `${CDN_BASE}${p}` });
```
若日后提供 UMD/IIFE 全局脚本（无 `import.meta.url`），则需显式 `setBaseUrl(cdnBase)` 配置兜底。

### 5. 打包约束（补充第九节）
- `@file-preview/browser` 用 tsup 产出 **ESM**（`format: ['esm']`），`assetsInclude` 把 wasm/worker 复制到 `assets/`。
- 该包 `sideEffects` 仅保留 customElements 注册副作用，其余可 tree-shake。

---

## 十六、生产就绪（错误码 / 超时 / CSP / 日志）

本方案定位 P0 生产可用，以下四项须落到接口与文档。

### 1. 错误码体系
`kind:'error'` 的 `code` 必须是**稳定枚举**（不随文案变）：

| code | 含义 | 调用方处理 |
| --- | --- | --- |
| `ERR_UNSUPPORTED` | 无插件可处理 | 降级二进制 / 提示"格式不支持" |
| `ERR_TOO_LARGE` | 超过 `maxBytes` | 降级元数据 / 十六进制 |
| `ERR_PARSE` | 插件解析失败 | 尝试下一个插件 / 报错 |
| `ERR_ABORTED` | 被 `AbortSignal` 取消 | 调用方感知中断 |
| `ERR_TIMEOUT` | 超过默认超时 | 中断并返回错误 |

调用方据此做可预期 UI（如"文件过大""格式不支持"），而非靠字符串匹配。

### 2. 默认超时
`PreviewOptions.timeout`（默认 30000ms）：核心在 `preview()` 内用 `combineSignal(opts.signal, AbortSignal.timeout(timeout))` 合并，任何插件超时被中止并返回 `ERR_TIMEOUT`。防止畸形/恶意文件（如损坏 PDF）挂起主线程或 Worker。

### 3. CSP 要求（托管页需配置）
渲染产物依赖下列资源，严格 CSP 会拦截，必须文档化：
- `img-src`：允许 `blob:`、`data:`（图片 / SVG / 缩略图）
- `media-src`：允许 `blob:`（音视频播放）
- `frame-src` / `child-src`：允许 `blob:`（`<iframe srcdoc>` 隔离 Office/markdown 整页 HTML）
- `worker-src` / `script-src`：允许 CDN 域（跨域加载 `pdf.worker.min.mjs` 需该 CDN 带 `Access-Control-Allow-Origin`）

建议随包提供一份最小可用 CSP 片段，以及自托管 wasm/worker 的部署说明。

### 4. 结构化日志
`EnvAdapter.log?(level, msg, err)`（第三节已加）：插件失败、降级、超时经此上报，不再裸 `console.error`。生产环境注入上报（Sentry / OpenTelemetry 等）。golden-file 测试之外，补充**错误码回归测试**，断言各 `code` 在对应场景被返回。

---

## 十七、核心竞争力（更新）

- **同构 + 流式**：一套代码跑浏览器与 Node，大文件不爆内存。
- **插件化 + 按需加载**：格式可插拔，重依赖不进主包。
- **统一结果模型 + 渲染层强制 sanitize**：解析与渲染解耦，安全是唯一清理点。
- **纯 JS/WASM、无原生模块**：部署简单，许可证干净（避开 SheetJS 地雷；mediainfo.js 为 BSD-2-Clause）。
- **开源治理就绪**：contractVersion + changesets + 体积预算，可长期维护。
- **零构建 HTML 引入**：CDN 上一行 module script + `<file-preview>` 即用，免 npm/打包器。
- **生产就绪**：稳定错误码 + 默认超时 + CSP 文档 + 结构化日志，可上生产。
